#!/bin/bash


# sudo dpkg --configure -a
# sudo apt-get install -fy
#
# sudo apt-get install -y software-properties-common
# sudo apt-add-repository -y ppa:dreibh/ppa
# sudo apt-get update
# sudo apt-get dist-upgrade -y
#
# sudo apt-get install -fy
# sudo apt-get dist-upgrade -y
#
# sudo service nornet-tunnelbox configure
#
# sudo apt-get install -y linux-generic-lts-wily


sudo apt-add-repository -y -s ppa:dreibh/ppa
sudo apt-get update
sudo apt-get install -y nornet-ca
sudo apt-get dist-upgrade -y

sudo apt-get purge -y 'virtualbox.*' tracebox stab
sudo apt-get install -y virtualbox-guest-utils

sudo rm -rf /etc/squid3/
sudo rm -rf /var/lib/dhcp/
sudo rm -f /var/run/dhcp-server/dhcpd.pid
