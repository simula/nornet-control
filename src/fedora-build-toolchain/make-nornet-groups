#!/bin/bash -e

add-group ()
{
   FILE="$1"
   NAME="$2"
   ID="$3"
   shift ; shift ; shift
   if [ -e "$FILE" ] ; then
      yum-groups-manager --merge "$FILE" "--name=$NAME" "--id=$ID" $@
   else
      yum-groups-manager --save  "$FILE" "--name=$NAME" "--id=$ID" $@
   fi
}


rm -f groups.xml

add-group nornet-groups.xml "extensionNorNetManagement" "extensionnornetmanagement" \
   rpm-sign \
   bind-utils \
   colordiff \
   ethtool \
   git \
   htop \
   idn \
   joe \
   kernel-modules-extra \
   lksctp-tools \
   netperfmeter \
   net-snmp-utils \
   nfs-utils \
   nmap \
   nuttcp \
   rsplib-tools \
   rsplib-services \
   subnetcalc \
   tcpdump \
   tftp \
   whois \
   xauth \
   yum-plugin-fastestmirror

add-group nornet-groups.xml "extensionNorNetNode" "extensionnornetnode" \
   openssh-server \
   open-vm-tools \
   smartmontools
