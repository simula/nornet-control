Index: debian/control
===================================================================
--- debian/control	(revision 45822)
+++ debian/control	(working copy)
@@ -3,23 +3,74 @@
 Priority: optional
 Maintainer: Oracle Corporation <info@virtualbox.org>
 Standards-Version: 3.2.1
-Build-Depends: debhelper (>= 5), libz-dev, libssl-dev, libqt4-dev,
-  libqt4-opengl-dev, libsdl1.2-dev, uuid-dev, libxcursor-dev,
-  libidl-dev, libpam0g-dev, iasl, xsltproc, linux-headers-generic,
-  wine, java2-runtime, lsb-release, g++-multilib [amd64],
-  libc6-dev-i386 [amd64], ia32-libs [amd64], libasound-dev,
-  libpulse-dev, libxml2-dev, python-dev, gsoap,
-  libcap2-dev, libgl1-mesa-dev, libglu1-mesa-dev, libxmu-dev,
-  libxext-dev, libcurl-dev, libxrandr-dev, openjdk-6-jdk | sun-java6-jdk
+Build-Depends: bcc,
+               debhelper (>= 5),
+               default-jdk,
+               g++,
+               g++-multilib,
+               gcc,
+               gcc-multilib,
+               ia32-libs,
+               iasl,
+               kbuild,
+               lib32gcc1,
+               lib32stdc++6,
+               libasound2-dev,
+               libc6-dev-i386,
+               libcap-dev,
+               libcurl4-openssl-dev,
+               libdevmapper-dev,
+               libglu1-mesa-dev,
+               libhal-dev,
+               libidl-dev,
+               libpam0g-dev,
+               libpulse-dev,
+               libqt4-dev,
+               libqt4-opengl-dev,
+               libsdl1.2-dev,
+               libstdc++5,
+               libvncserver-dev,
+               libxcursor-dev,
+               libxinerama-dev,
+               libxml2-dev,
+               libxmu-dev,
+               libxrandr-dev,
+               libxslt1-dev,
+               linux-kernel-headers,
+               makeself,
+               mesa-common-dev,
+               mkisofs,
+               python-central,
+               python-dev,
+               qt4-dev-tools,
+               texlive-fonts-extra,
+               texlive-fonts-recommended,
+               texlive-latex-base,
+               texlive-latex-extra,
+               texlive-latex-recommended,
+               uuid-dev,
+               xsltproc,
+               zip,
+               zlib1g-dev
 XS-Python-Version: >= 2.4
 
 Package: virtualbox-ose
-Architecture: i386 amd64
+Architecture: amd64 i386
 Pre-Depends: debconf (>= 1.1) | debconf-2.0
-Depends: ${shlibs:Depends}, debconf (>= 0.5) | debconf-2.0, psmisc, adduser
-Recommends: ${alsa}, ${pulse}, ${sdlttf}, ${dkms},
-  linux-headers, gcc, make, binutils,
-  python-central
+Depends: adduser,
+         debconf (>= 0.5) | debconf-2.0,
+         psmisc,
+         ${shlibs:Depends}
+Provides: virtualbox
+Recommends: binutils,
+            gcc,
+            linux-headers,
+            make,
+            python-central,
+            ${alsa},
+            ${dkms},
+            ${pulse},
+            ${sdlttf}
 Description: Oracle VM VirtualBox
  VirtualBox is a powerful PC virtualization solution allowing you to run a
  wide range of PC operating systems on your Linux system. This includes
@@ -29,7 +80,7 @@
 XB-Python-Version: ${python:Versions}
 
 Package: virtualbox-ose-dbg
-Architecture: i386 amd64
+Architecture: amd64 i386
 Depends: virtualbox-ose (= ${Source-Version})
 Description: Oracle VM VirtualBox debugging symbols
  VirtualBox is a powerful PC virtualization solution allowing you to run a
Index: debian/LocalConfig.kmk
===================================================================
--- debian/LocalConfig.kmk	(revision 45822)
+++ debian/LocalConfig.kmk	(working copy)
@@ -29,3 +29,5 @@
 VBOX_WITH_RUNPATH          := $(VBOX_PATH_APP_PRIVATE_ARCH)
 VBOX_PATH_APP_PRIVATE      := /usr/share/virtualbox
 VBOX_PATH_APP_DOCS          = $(VBOX_PATH_PACKAGE_DOCS)
+
+VBOX_WITH_EXTPACK_VNC := 1
Index: debian/changelog
===================================================================
--- debian/changelog	(revision 45822)
+++ debian/changelog	(working copy)
@@ -1,4 +1,4 @@
-virtualbox-ose (4.2.51~svn-1) unstable; urgency=low
+virtualbox-ose (4.2.51-1ubuntu1) unstable; urgency=low
 
   * rebuild Debian package
 
Index: debian/rules
===================================================================
--- debian/rules	(revision 45822)
+++ debian/rules	(working copy)
@@ -28,7 +28,7 @@
 #  NOQT=1           don't build the Qt GUI
 #  EFI=1            include the EFI binary from prebuild
 #  HEADLESS=1       build the headless version
-#  VNC=1            build VNC code
+VNC=1            build VNC code
 #  NOWEBSVC=1       don't build the webservice API, default for OSE
 #  STAGEDISO=<path> don't build the VBoxAdditions, use the precompiled .iso
 #  PKGDIR=<path>    where to store the final package(s)
@@ -78,7 +78,8 @@
 	     $(if $(NOWINE),,--setup-wine) \
 	     $(if $(NOWEBSVC),,$(if $(ose),--enable-webservice,)) \
 	     $(if $(VNC),--enable-vnc,) \
-	     --disable-extpack
+	     --disable-extpack \
+	     --disable-kmods
 
 bld_flags := AUTOCFG=$(current)/debian/AutoConfig.kmk \
 	     LOCALCFG=$(current)/debian/LocalConfig.kmk \
Index: Config.kmk
===================================================================
--- Config.kmk	(revision 45822)
+++ Config.kmk	(working copy)
@@ -580,7 +580,7 @@
  VBOX_WITH_CROGL = 1
 endif
 # Enable the VNC server extension pack (GPL only).
-#VBOX_WITH_EXTPACK_VNC = 1
+VBOX_WITH_EXTPACK_VNC = 1
 # Enable S3 support (requires libcurl)
 VBOX_WITH_S3 = 1
 # Enable Host=>Guest Drag'n'Drop
