#!/usr/bin/python
# -*- coding: utf-8 -*-

import sys;
import re;

i = 1
while i < len(sys.argv):
   match = re.search('^--user=(.*)', sys.argv[i])
   if match != None:
      print 'USR=',match.group(1)

   i = i + 1
